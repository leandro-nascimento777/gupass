import { PlaceholderPage } from '@/components/shared/PlaceholderPage'

export function VendasPage() {
  return <PlaceholderPage title="Vendas" description="Listagem de vendas + wizard de 5 passos: Cliente, Origem, Itens, Pagamento, Confirmar." />
}
